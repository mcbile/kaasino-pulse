# ===========================================
# Product Pulse - Environment Configuration
# ===========================================

# --------------------------------------------
# Frontend (Vite)
# --------------------------------------------

# Google OAuth Client ID
# Get from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Backend API URL (for frontend to connect)
VITE_API_URL=http://localhost:8080

# --------------------------------------------
# Backend (Go Collector)
# --------------------------------------------

# Server
PORT=8080

# Database (TimescaleDB/PostgreSQL)
DATABASE_URL=postgres://user:password@localhost:5432/pulse?sslmode=disable

# Batch processing
BATCH_SIZE=100
FLUSH_INTERVAL=5s
WORKERS=4

# CORS
ALLOWED_ORIGINS=http://localhost:3001,https://pulse-dashboard.onrender.com

# Debug mode
DEBUG=false

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_RPS=100
RATE_LIMIT_BURST=200

# Request limits
MAX_BODY_SIZE=1048576

# --------------------------------------------
# Authentication
# --------------------------------------------

# Admin users (REQUIRED for login)
# Format: email:password_hash:name:nickname
# Multiple users: email1:hash1:name1:nick1,email2:hash2:name2:nick2
#
# To generate password hash (SHA256):
#   echo -n "YourPassword" | sha256sum | cut -d' ' -f1
#
# Example for password "MySecurePass123":
#   echo -n "MySecurePass123" | sha256sum
#   => a1b2c3d4e5f6... (use this hash)
#
ADMIN_USERS=admin@example.com:your_sha256_hash_here:Admin:admin
